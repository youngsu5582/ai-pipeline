# 아키텍처

AI Pipeline의 시스템 구조와 설계 철학을 설명합니다.

## 설계 철학

### 1. Zero-Friction (마찰 최소화)
- 한 줄 명령어로 기록 (`quick "메모"`)
- 저장 전 미리보기 + 수정 가능
- 자동 분류, 자동 연결

### 2. 지식 + 맥락 (Context-Aware)
- 단순 "뭘 배웠다"가 아닌 "왜, 어떤 상황에서"
- 고민/생각 섹션으로 맥락 보존
- 주간/월간 회고에서 맥락 분석

### 3. 점진적 정제 (Progressive Refinement)
```
raw (YYYY/MM/DD) → _drafts (staging) → study (최종)
```
- 모든 노트는 staging 단계를 거침
- 수정/검토 기회 제공
- 품질 관리 가능

## 데이터 흐름

```
                        ┌──────────────────────────────────────────┐
                        │              Input Sources               │
                        └──────────────────────────────────────────┘
                                          │
        ┌─────────────────┬───────────────┼───────────────┬─────────────────┐
        ▼                 ▼               ▼               ▼                 ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│  AI 대화 로그  │ │  quick notes  │ │  Daily Note   │ │ GitHub 활동   │ │  직접 작성    │
│  (raw/YY/MM/DD)│ │   (quick)     │ │ (daily-init)  │ │(sync-github)  │ │  (Obsidian)   │
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                 │               │               │                 │
        ▼                 ▼               │               │                 │
┌───────────────────────────────┐         │               │                 │
│         _drafts/              │         │               │                 │
│    (Staging Area)             │◀────────┘               │                 │
│                               │                         │                 │
│  YYYY-MM-DD_제목.md           │                         │                 │
│  YYYY-MM-DD_quick-notes.md    │                         │                 │
└───────────────┬───────────────┘                         │                 │
                │                                         │                 │
                ▼                                         ▼                 │
┌───────────────────────────────────────────────────────────────┐          │
│                      Daily Note                                │          │
│                 YYYY-MM-DD.md                                  │◀─────────┘
│                                                                │
│  🎯 오늘의 Focus                                                │
│  📋 할 일                                                       │
│  🤔 고민거리        ◀── 맥락 정보 (weekly/monthly에서 분석)      │
│  📝 오늘의 생각     ◀── 맥락 정보                                │
│  🐙 GitHub 활동     ◀── sync-github                             │
│  🤖 오늘 배운 것    ◀── ai-daily                                 │
│  ✅ 오늘 한 일                                                   │
└───────────────────────────────┬───────────────────────────────┘
                                │
                                ▼
┌───────────────────────────────────────────────────────────────┐
│                      Weekly Review                             │
│                    (ai-weekly)                                 │
│                                                                │
│  Input:                                                        │
│    - Draft Notes                                               │
│    - Daily Notes                                               │
│    - Quick Notes                                               │
│    - 고민/생각 섹션                                              │
│                                                                │
│  Output:                                                       │
│    - YYYY-WXX-회고.md (주제별 정리, 도전과 배움, 다음 주 액션)    │
│    - YYYY-WXX-quiz.md (복습 퀴즈 5개)                           │
└───────────────────────────────┬───────────────────────────────┘
                                │
                                ▼
┌───────────────────────────────────────────────────────────────┐
│                     Monthly Report                             │
│                    (ai-monthly)                                │
│                                                                │
│  Input:                                                        │
│    - 해당 월의 주간 회고들                                       │
│    - Daily Notes (고민/생각)                                    │
│    - Quick Notes                                               │
│    - GitHub 활동                                                │
│                                                                │
│  Output:                                                       │
│    - YYYY-MM-월간리포트.md                                       │
│      (성장 영역, 도전, 반복 패턴, 다음 달 Focus)                  │
└───────────────────────────────────────────────────────────────┘
```

## 폴더 구조

```
Obsidian Vault/
├── DAILY/                          # 일일 노트
│   ├── 2026-01-16.md              # Daily Note
│   ├── 2026-W03-회고.md           # 주간 회고
│   └── 2026-01-월간리포트.md       # 월간 리포트
│
└── study/
    ├── _drafts/                    # Staging Area
    │   ├── 2026-01-16_Spring-Boot-설정.md
    │   └── 2026-01-16_quick-notes.md
    │
    ├── _quizzes/                   # 복습 퀴즈
    │   └── 2026-W03-quiz.md
    │
    ├── Java/                       # 최종 지식 (주제별)
    ├── Docker/
    ├── kafka/
    └── ...
```

## 모듈 구조

```
ai-pipeline/
├── config/
│   ├── settings.example.yaml      # 템플릿
│   └── settings.local.yaml        # 개인 설정 (gitignore)
│
├── scripts/
│   ├── config.py                  # 설정 로딩 (환경변수 + yaml)
│   │
│   ├── quick.py                   # quick 명령어
│   ├── daily.py                   # daily-init, ai-daily
│   ├── weekly.py                  # ai-weekly
│   ├── monthly.py                 # ai-monthly
│   ├── sync_github.py             # sync-github
│   ├── processor.py               # ai-process
│   │
│   └── aliases.sh                 # Shell 별칭
│
├── docs/
│   ├── SETUP.md
│   ├── COMMANDS.md
│   └── ARCHITECTURE.md
│
├── requirements.txt
└── README.md
```

## 설정 시스템

### 우선순위
```
1. 환경변수 (최우선)
2. config/settings.local.yaml
3. config/settings.yaml
4. config/settings.example.yaml (fallback)
```

### 환경변수 매핑
```python
env_mappings = {
    "AI_VAULT_PATH": ("vault", "path"),
    "AI_TARGET_FOLDER": ("vault", "target_folder"),
    "AI_DRAFTS_FOLDER": ("vault", "drafts_folder"),
    "AI_DAILY_FOLDER": ("vault", "daily_folder"),
    "AI_LLM_PROVIDER": ("llm", "provider"),
    "AI_GITHUB_REPOS": ("github", "repos"),  # 콤마 구분
}
```

## LLM 통합

### 지원 Provider
| Provider | 모델 | Context | 비용 |
|----------|------|---------|------|
| Gemini | gemini-2.0-flash | 2M tokens | 무료 tier |
| OpenAI | gpt-4o-mini | 128K tokens | 유료 |
| Anthropic | claude-3-5-haiku | 200K tokens | 유료 |

### 사용처
- **processor.py**: AI 대화 로그 → 구조화된 노트
- **weekly.py**: 주간 노트 → 회고 + 퀴즈
- **monthly.py**: 월간 데이터 → 성장 리포트

### 프롬프트 설계
- JSON 출력 강제 (`response_mime_type="application/json"`)
- 명확한 스키마 정의
- 맥락 정보 포함 (고민/상황)

## GitHub 통합

### 데이터 수집
```python
# 1. GitHub API Events
events = gh api users/{username}/events

# 지원 이벤트:
# - PushEvent → commits
# - PullRequestEvent → PRs
# - PullRequestReviewEvent → reviews
# - PullRequestReviewCommentEvent → comments
# - IssueCommentEvent → comments

# 2. 로컬 git log (더 정확한 커밋 정보)
git log --author={email} --since={date} --until={date}
```

### 중복 제거
- SHA 기준 커밋 중복 제거
- amend된 커밋은 최종본만 (git log 기반)

## 확장 포인트

### 새 명령어 추가
1. `scripts/` 에 새 스크립트 생성
2. `aliases.sh`에 별칭 추가
3. `docs/COMMANDS.md` 문서화

### 새 LLM Provider 추가
1. `processor.py`에 클라이언트 클래스 추가
2. `config/settings.example.yaml`에 설정 추가
3. API 키 환경변수 문서화

### 새 데이터 소스 추가
1. 수집 스크립트 생성
2. Daily Note 섹션 정의
3. weekly/monthly 분석에 포함
