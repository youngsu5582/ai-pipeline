# 흐름도

주요 시나리오별 상세 흐름을 설명합니다.

## 1. 작업 실행 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         작업 실행 트리거                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐              │
│   │   수동 실행   │   │  스케줄 실행  │   │  체이닝 실행  │              │
│   │ POST /run    │   │  node-cron   │   │ triggerNext  │              │
│   └──────┬───────┘   └──────┬───────┘   └──────┬───────┘              │
│          │                  │                  │                       │
│          └────────────┬─────┴──────────────────┘                       │
│                       ▼                                                 │
│              ┌────────────────┐                                         │
│              │  executeJob()  │                                         │
│              └────────┬───────┘                                         │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 1. 동시 실행 체크 (runningJobs[job.id] 확인)            │          │
│   │    └─ 이미 실행 중이면 reject                           │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 2. 명령어 빌드                                          │          │
│   │    buildCommand(job, options)                          │          │
│   │    └─ boolean: --flag                                  │          │
│   │    └─ string: --flag "value"                           │          │
│   │    └─ array: --flag "v1,v2,v3"                         │          │
│   │    └─ select: --flag "selected"                        │          │
│   │    └─ system 옵션은 제외                                │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 3. 로그 엔트리 생성                                      │          │
│   │    { id, jobId, jobName, trigger, startTime,           │          │
│   │      status: 'running', stdout, stderr, command }      │          │
│   │    jobHistory.push(logEntry)                           │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 4. 프로세스 실행                                         │          │
│   │    spawn('/bin/zsh', ['-c', command])                  │          │
│   │    └─ env: PATH 확장 (/usr/local/bin, /opt/homebrew)  │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 5. 실행 중 모니터링                                      │          │
│   │    ├─ stdout.on('data') → logEntry.stdout 축적         │          │
│   │    ├─ stderr.on('data') → logEntry.stderr 축적         │          │
│   │    └─ 타임아웃 타이머 설정 (timeout ms 후 SIGTERM)      │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 6. 완료 처리 (child.on('close'))                        │          │
│   │    ├─ 타임아웃? → status='failed', error 설정          │          │
│   │    ├─ code !== 0?                                      │          │
│   │    │   ├─ Auto-fix 확인                                │          │
│   │    │   │   └─ 패턴 매칭 → 복구 명령 실행 → 재실행      │          │
│   │    │   ├─ 재시도 횟수 남음?                            │          │
│   │    │   │   └─ setTimeout → executeJob(retry)           │          │
│   │    │   └─ 최종 실패 → Slack 알림                       │          │
│   │    └─ code === 0?                                      │          │
│   │        └─ status='success' → Slack 알림                │          │
│   └─────────────────────────────────────────────────────────┘          │
│                       │                                                 │
│                       ▼                                                 │
│   ┌─────────────────────────────────────────────────────────┐          │
│   │ 7. 후처리                                                │          │
│   │    ├─ saveHistory() → logs/history.json 저장           │          │
│   │    ├─ delete runningJobs[job.id]                       │          │
│   │    └─ triggerNextJobs() → 파이프라인 체이닝             │          │
│   └─────────────────────────────────────────────────────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 2. 재시도 및 Auto-fix 흐름

```
작업 실패 (code !== 0)
        │
        ▼
┌───────────────────────────┐
│ retryAttempt === 0?       │──No──┐
└───────────┬───────────────┘      │
            │Yes                    │
            ▼                       │
┌───────────────────────────┐      │
│ checkAutoFix(stdout,      │      │
│              stderr)      │      │
└───────────┬───────────────┘      │
            │                       │
    ┌───────┴───────┐              │
    │               │              │
    ▼ 패턴 매칭     ▼ 매칭 없음    │
┌──────────┐    ┌──────────┐       │
│ Auto-fix │    │ 일반     │       │
│ 실행     │    │ 재시도   │◄──────┘
└────┬─────┘    └────┬─────┘
     │               │
     ▼               ▼
┌──────────┐   ┌─────────────────────────┐
│ pip/npm  │   │ retryAttempt < maxRetries?│
│ install  │   └────────────┬────────────┘
└────┬─────┘                │
     │                 Yes  │  No
     │                  ┌───┴───┐
     │                  ▼       ▼
     │        ┌──────────┐  ┌──────────┐
     │        │ 재시도   │  │ 최종실패 │
     │        │ 대기     │  │ → Slack  │
     │        │ (delay)  │  └──────────┘
     │        └────┬─────┘
     │             │
     └──────┬──────┘
            │
            ▼
    executeJob(job, 'retry/auto-fix')


백오프 계산:
- fixed: delay (고정)
- linear: delay × attempt
- exponential: delay × 2^(attempt-1)
```

## 3. 파이프라인 체이닝 흐름

```
jobs.json edges:
[
  { from: "A", to: "B", trigger: true, onSuccess: true },
  { from: "A", to: "C", trigger: true, onSuccess: false },
  { from: "B", to: "D", trigger: true, onSuccess: true }
]

실행 흐름:

Job A 실행
    │
    ▼
┌──────────────┐
│ A 완료 처리  │
│ status=?     │
└──────┬───────┘
       │
       ▼
triggerNextJobs("A", status, log, depth=0)
       │
       ├──────────────────────────────────┐
       │                                  │
       ▼                                  ▼
┌──────────────────┐            ┌──────────────────┐
│ Edge A→B         │            │ Edge A→C         │
│ trigger: true    │            │ trigger: true    │
│ onSuccess: true  │            │ onSuccess: false │
└────────┬─────────┘            └────────┬─────────┘
         │                               │
         │ (A가 성공한 경우만)            │ (항상 실행)
         ▼                               ▼
┌────────────────┐              ┌────────────────┐
│ Job B 실행     │              │ Job C 실행     │
│ trigger=chained│              │ trigger=chained│
│ depth=1        │              │ depth=1        │
└────────┬───────┘              └────────────────┘
         │
         ▼
┌──────────────┐
│ B 완료 처리  │
└──────┬───────┘
       │
       ▼
triggerNextJobs("B", status, log, depth=1)
       │
       ▼
┌──────────────────┐
│ Edge B→D         │
│ trigger: true    │
│ onSuccess: true  │
└────────┬─────────┘
         │
         ▼
┌────────────────┐
│ Job D 실행     │
│ depth=2        │
└────────────────┘


depth > 10 → 무한 루프 방지, 중단
```

## 4. 인터랙티브 작업 흐름 (Electron)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      InteractiveJobRunner                               │
└─────────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 1. start()                                                              │
│    ├─ loadJobs() - jobs.json에서 interactive=true 작업 필터             │
│    └─ scheduleJobs() - node-cron으로 스케줄 등록                        │
└─────────────────────────────────────────────────────────────────────────┘
                                │
                                │ 스케줄 트리거
                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 2. executeJob(job)                                                      │
│                                                                         │
│    ┌─────────────────────────────────────────┐                         │
│    │ 알림 설정 확인                           │                         │
│    │ - enabled?                              │                         │
│    │ - 시간대 (startHour ~ endHour)?         │                         │
│    └─────────────────────────────────────────┘                         │
│                    │                                                    │
│                    │ 조건 충족                                          │
│                    ▼                                                    │
│    ┌─────────────────────────────────────────┐                         │
│    │ collectData(job)                        │                         │
│    │ - todayEntries: store.get('entries')    │                         │
│    │ - claudeSessions: sessionCollector      │                         │
│    └─────────────────────────────────────────┘                         │
│                    │                                                    │
│                    ▼                                                    │
│    ┌─────────────────────────────────────────┐                         │
│    │ popupWindow.show(job, collectedData)    │◄──── 팝업 표시          │
│    │                                         │                         │
│    │ Promise 대기 (사용자 입력)              │                         │
│    └─────────────────────────────────────────┘                         │
└─────────────────────────────────────────────────────────────────────────┘
                                │
           ┌────────────────────┼────────────────────┐
           │                    │                    │
           ▼                    ▼                    ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │ 입력 완료    │    │ 스킵         │    │ 시간 초과    │
    │              │    │              │    │ (리마인더)   │
    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘
           │                   │                   │
           │                   │                   │
           ▼                   │                   ▼
┌────────────────────┐         │          ┌────────────────────┐
│ 3. Claude 처리     │         │          │ 리마인더 팝업      │
│    (processing.    │         │          │ (reminderMinutes   │
│     claude.enabled)│         │          │  후 재시도)        │
│                    │         │          └────────────────────┘
│ claudeCode.ask(    │         │
│   input,           │         │
│   { system: prompt}│         │
│ )                  │         │
└────────┬───────────┘         │
         │                     │
         ▼                     │
┌────────────────────┐         │
│ 4. 저장            │         │
│    saveOutput(job, │         │
│      content)      │         │
│                    │         │
│ - obsidianWriter.  │         │
│   appendToSection()│         │
│ - store.set(       │         │
│   'entries.today', │         │
│   [...])           │         │
└────────────────────┘         │
         │                     │
         └─────────┬───────────┘
                   │
                   ▼
           ┌───────────────┐
           │ 완료          │
           └───────────────┘
```

## 5. Obsidian 저장 흐름

```
ObsidianWriter.appendToSection(section, content)
                    │
                    ▼
        ┌───────────────────────┐
        │ 1. Daily Note 경로    │
        │    getDailyNotePath() │
        │    → vault/DAILY/     │
        │      2026-02-06.md    │
        └───────────┬───────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │ 2. 파일 존재 확인     │
        │    ensureDailyNote()  │
        └───────────┬───────────┘
                    │
          ┌─────────┴─────────┐
          │                   │
          ▼ 있음              ▼ 없음
   ┌─────────────┐    ┌─────────────────────┐
   │ 파일 읽기   │    │ 템플릿으로 생성     │
   └──────┬──────┘    │ createDailyNote     │
          │           │ Template()          │
          │           └──────────┬──────────┘
          │                      │
          └──────────┬───────────┘
                     │
                     ▼
        ┌───────────────────────┐
        │ 3. 섹션 찾기          │
        │    regex: /## 섹션명/ │
        └───────────┬───────────┘
                    │
          ┌─────────┴─────────┐
          │                   │
          ▼ 있음              ▼ 없음
   ┌─────────────────┐  ┌─────────────────┐
   │ 섹션 바로 뒤에  │  │ 파일 끝에       │
   │ 내용 삽입       │  │ 섹션 + 내용 추가│
   └────────┬────────┘  └────────┬────────┘
            │                    │
            └──────────┬─────────┘
                       │
                       ▼
        ┌───────────────────────┐
        │ 4. 파일 저장          │
        │    fs.writeFileSync() │
        └───────────────────────┘


Daily Note 템플릿:
---
date: 2026-02-06
weekday: 목요일
---

# 2026-02-06 (목)
> 어제: [[2026-02-05]]

## 🎯 오늘의 Focus
## 📋 할 일
## 🤔 고민거리
## 📝 오늘의 생각
## ⏰ 시간별 메모    ◄── appendHourlyEntry()
## ✅ 오늘 한 일      ◄── replaceSection()
## 🤖 Claude 세션 요약 ◄── replaceSection()
```

## 6. SSE 실시간 이벤트 흐름

```
클라이언트                           서버
    │                                 │
    │  GET /api/tasks/events          │
    │  ?clientId=client-123           │
    │ ─────────────────────────────► │
    │                                 │
    │  Headers:                       │
    │  Content-Type: text/event-stream│
    │  Connection: keep-alive         │
    │ ◄───────────────────────────── │
    │                                 │
    │  event: connected               │
    │  data: {"clientId":"client-123"}│
    │ ◄───────────────────────────── │
    │                                 │
    │       [연결 유지]               │
    │                                 │
    │  event: task:progress           │
    │  data: {"taskId":"...",         │
    │         "progress":50,          │
    │         "message":"진행중"}     │
    │ ◄───────────────────────────── │
    │                                 │
    │  event: ping                    │
    │ ◄───────────────────────────── │ (30초마다)
    │                                 │
    │       ...                       │
    │                                 │
    │  event: task:complete           │
    │  data: {"taskId":"...",         │
    │         "status":"success"}     │
    │ ◄───────────────────────────── │
    │                                 │


서버 측:
sseClients.set(clientId, res);

sendSSEEvent(clientId, 'task:progress', {
  taskId,
  progress: 50,
  message: '진행 중'
});
```

## 7. UI 상태 전환

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            대시보드 UI                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         탭 전환                                   │   │
│  │  [작업 목록] [실행 이력] [통계] [설정] [세션]                   │   │
│  └────────────────────────────┬────────────────────────────────────┘   │
│                               │                                         │
│         ┌─────────────────────┼─────────────────────┐                  │
│         │                     │                     │                  │
│         ▼                     ▼                     ▼                  │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐            │
│  │ panel-jobs  │      │ panel-      │      │ panel-stats │            │
│  │             │      │ history     │      │             │            │
│  │ ┌─────────┐ │      └─────────────┘      │ - 요약 카드 │            │
│  │ │ 뷰 전환 │ │                           │ - 트렌드    │            │
│  │ │[카드]   │ │                           │ - 시간대별  │            │
│  │ │[그래프] │ │                           │ - 작업별    │            │
│  │ └────┬────┘ │                           └─────────────┘            │
│  │      │      │                                                       │
│  │   ┌──┴──┐   │                                                       │
│  │   │     │   │                                                       │
│  │   ▼     ▼   │                                                       │
│  │ 카드   그래프│                                                       │
│  │ 뷰     뷰   │                                                       │
│  └─────────────┘                                                       │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         모달                                      │   │
│  │  - addModal (작업 추가/수정)                                     │   │
│  │  - runModal (실행 옵션)                                          │   │
│  │  - logModal (로그 상세)                                          │   │
│  │  - scheduleModal (예약 실행)                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         토스트                                    │   │
│  │  showToast(message, type) → #toastContainer                     │   │
│  │  5초 후 자동 닫기                                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘


상태 변수:
- currentTab: 'jobs' | 'history' | 'stats' | 'settings' | 'sessions'
- currentView: 'card' | 'graph'
- currentCategory: null | 'sync' | 'daily' | ...
- currentJobId: 선택된 작업 ID
```
